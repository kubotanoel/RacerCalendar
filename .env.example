DATABASE_URL="file:./dev.db"
# Min 16 characters in production; dev fallback exists only when NODE_ENV !== production
CALENDAR_FEED_SECRET=""
SESSION_SECRET=""
# Server origin for OAuth redirect (matches Google Console “Authorized redirect URIs”)
APP_ORIGIN="http://localhost:3000"
# Optional: used client-side only for documenting absolute URLs before hydration
NEXT_PUBLIC_APP_ORIGIN="http://localhost:3000"

# OAuth push sync (optional Phase 2+)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
